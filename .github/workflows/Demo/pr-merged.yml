name: PR Merged

on:
  pull_request:
    types: [closed]

jobs:
  say_hello_pr:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: Message
        run: echo "PR mergée"